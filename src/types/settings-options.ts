export type SettingsOptions = {
    /**
     * Custom Settings URL
     */
    url?: string
    /**
     * Array of fields to show in `<SettingsCards />`
     * @default ["avatar", "name"]
     */
    fields: string[]
}
